from setuptools import setup, find_packages

setup(name='qi',
      version='0.1',
      description='Qi"s Python package',
      author='Qi Mi',
      license='MIT',
      packages= find_packages(),
      zip_safe=False)
